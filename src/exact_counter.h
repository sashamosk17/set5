#pragma once

#include <string>
#include <vector>
#include <unordered_set>
#include <cstddef>

class ExactCounter {
 public:
  // ??????? ?????? ????? ?????????? ????????? ? ??????
  static size_t count(const std::vector<std::string>& stream);
};